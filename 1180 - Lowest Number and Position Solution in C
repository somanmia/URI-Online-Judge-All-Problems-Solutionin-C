#include<stdio.h>
int main(){
int n,i,pos,min,j;
scanf("%d",&n);
int array[n];
for(i=0;i<n;i++){
    scanf("%d",&array[i]);
}
min=array[0];
for(j=1;j<n;j++){
    if(array[j]<min){
        min=array[j];
        pos=j;
    }
}
printf("Menor valor: %d\n",min);
printf("Posicao: %d\n",pos);
return 0;
}
